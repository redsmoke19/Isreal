mixin icon(name,mod,wi,he)
  if(mod)
    svg(class="svg-sprite-icon icon-" + name + ' ' + mod width=+wi height=+he)
      use(xlink:href="img/sprite_auto.svg#" + name)
  else
    svg(class="svg-sprite-icon icon-" + name)
      use(xlink:href="img/sprite_auto.svg##" + name)
